/**
 * Feature flags — централизованные тумблеры функциональности.
 *
 * Зачем: иногда нужно временно скрыть фичу без удаления кода (например,
 * телефон + SMS — пока SMS-провайдер не настроен, флаг false).
 * Когда подключим — меняем константу на true и всё возвращается.
 *
 * Не env-переменные, чтобы Vite не пересобирал клиентский бандл при каждой
 * перестановке. Достаточно изменить значение тут и пересобрать как обычно.
 */
export const FEATURES = {
  // Верификация телефона через SMS, бейдж «Проверенный игрок», поле phone в /profile.
  // Пока SMS-провайдер не подключён — false. Включаем по факту настройки.
  PHONE_VERIFICATION: false,
} as const;
